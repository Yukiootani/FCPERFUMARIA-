[build]
  base = "."
  publish = "."
  functions = "netlify/functions"

# Garante que o celular sempre baixe a versão mais nova do App
[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache"

[[headers]]
  for = "/index.html"
  [headers.values]
    Cache-Control = "no-cache"
